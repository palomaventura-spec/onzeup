import {requireOrganizationUser} from "@/lib/auth";import {setTheme} from './actions';export default async function Settings(){const u=await requireOrganizationUser();return <><div className="page-head"><div><h1>Configurações</h1><p className="muted">Preferências de uso do painel.</p></div></div><section className="card"><form className="form" action={setTheme}><label>Tema do painel<select name="panelTheme" defaultValue={u.organization?.panelTheme||'DARK'}><option value="DARK">Escuro</option><option value="LIGHT">Claro</option><option value="SYSTEM">Seguir sistema</option></select></label><button>Salvar preferência</button></form><p className="help">O tema será aplicado de forma completa na próxima rodada visual; a preferência já fica registrada por organização.</p></section></> }