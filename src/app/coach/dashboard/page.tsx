import Link from "next/link"; import CopyInviteLink from "@/components/CopyInviteLink"; import { requireUser } from "@/lib/auth"; import { prisma } from "@/lib/prisma"; import { redirect } from "next/navigation";
export default async function CoachDashboard(){const user=await requireUser();if(user.role!=="COACH")redirect(user.role==="GUARDIAN"?"/responsavel":"/dashboard");const coach=await prisma.coachProfile.findUnique({where:{ownerUserId:user.id},include:{_count:{select:{referrals:true}},referrals:{include:{user:true},orderBy:{createdAt:"desc"},take:8}}});if(!coach)redirect("/cadastro-coach");const invite=`https://onzeup.com.br/convite/${coach.slug}`;return <main className="coach-partner-dashboard"><header><div><span className="page-eyebrow">ONZEUP COACH • PARCEIRO</span><h1>Olá, {coach.professionalName||coach.name}.</h1><p>Seu perfil profissional é gratuito. Use a ONZEUP para apresentar sua trajetória e indicar Player às famílias.</p></div><Link className="btn-secondary" href="/coach/editar">Editar meu perfil</Link></header><section className="coach-dashboard-stats"><article><small>ATLETAS INDICADOS</small><strong>{coach._count.referrals}</strong><span>cadastros atribuídos ao seu link</span></article><article><small>PERFIL PÚBLICO</small><strong>{coach.isPublic?"ATIVO":"RASCUNHO"}</strong><span>{coach.directoryVisible?"Visível no catálogo":"Fora do catálogo"}</span></article><article><small>ORGANIZAÇÃO</small><strong>{coach.managesOrganization?"SIM":"NÃO"}</strong><span>{coach.managedOrganizationType||"Você pode informar a qualquer momento"}</span></article></section><div className="coach-dashboard-grid"><section className="card coach-referral-box"><span className="page-eyebrow">INDIQUE UM ATLETA</span><h2>Seu link de parceiro</h2><p>Envie este link aos responsáveis. O Player começa grátis e o cadastro fica associado à sua indicação.</p><CopyInviteLink value={invite}/><a className="btn" href={invite} target="_blank">Abrir convite ↗</a></section><section className="card coach-club-lead"><span className="page-eyebrow">TAMBÉM GERE UMA ESCOLINHA OU CT?</span><h2>Leve sua gestão para o ONZEUP Club.</h2><p>Atletas, treinos, jogos, comunicação e financeiro em um só painel.</p><a className="btn" href="https://club.onzeup.com.br">Conhecer ONZEUP Club →</a></section></div><section className="card coach-referrals-list"><div><span className="page-eyebrow">MINHAS INDICAÇÕES</span><h2>Famílias que chegaram pelo seu link</h2></div>{coach.referrals.length?<div>{coach.referrals.map(r=><article key={r.id}><strong>{r.user.name}</strong><span>{r.user.email}</span><small>{new Intl.DateTimeFormat("pt-BR").format(r.createdAt)}</small></article>)}</div>:<p className="muted">Nenhuma indicação cadastrada ainda. Compartilhe seu link com as famílias dos atletas.</p>}</section></main>}
