import Link from "next/link";
export default async function AccessBlocked({searchParams}:{searchParams:Promise<{status?:string}>}){const {status}=await searchParams; const text=status==="suspended"?"O acesso desta organização está suspenso.":status==="cancelled"?"O acesso desta organização foi cancelado.":"O período de cortesia desta organização terminou.";return <main className="access-blocked-page"><section className="card"><span className="page-eyebrow">ONZEUP CLUB</span><h1>Acesso indisponível</h1><p>{text}</p><p className="muted">Entre em contato com a ONZEUP para revisar o status da conta.</p><form action="/api/auth/logout" method="post"><button className="btn">Sair</button></form><Link href="/">Voltar ao site</Link></section></main>}
