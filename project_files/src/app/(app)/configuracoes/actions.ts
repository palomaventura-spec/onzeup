"use server"; import {revalidatePath} from "next/cache";import {prisma} from "@/lib/prisma";import {requireOrganizationUser} from "@/lib/auth";export async function setTheme(f:FormData){const u=await requireOrganizationUser();const panelTheme=String(f.get('panelTheme')||'DARK');await prisma.organization.update({where:{id:u.organizationId},data:{panelTheme}});revalidatePath('/configuracoes')}